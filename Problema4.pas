program gradinita;
var N, S, X, Z:integer;
             M:real;
begin
     write('Introduce cate zile de recventa sunt:');
     readln(N);
     write('Introduce suma lunara:');
     readln(S);
     write('Introduce zilele lunii:');
     readln(z); 
     M:=(S/Z)*N;
     write('Suma pentru recventa:',M:5:2);
end.

     